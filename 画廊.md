```dataviewjs

// ================== 配置 ==================
const TYPES = ["book", "movie", "teleplay", "anime", "comic"];

let state = {
    type: "all",
    status: "all",
    rating: "all",
    time: "all",
    sort: "rating"
};

// ================== 容器 ==================
const container = dv.container;
container.innerHTML = "";

const filter = container.createEl("div", { cls: "filter-wrap" });
const grid = container.createEl("div", { cls: "grid" });

// ================== 数据 ==================
let pages = dv.pages().where(p => p.type);

// ================== 工具函数 ==================
function createBtn(parent, text, key, value) {
    let b = parent.createEl("button", { text, cls: "btn" });

    if (state[key] === value) b.addClass("active");

    b.onclick = () => {
        state[key] = value;
        render();
    };
}

// ================== 时间筛选 ==================
function timeFilter(p) {
    if (state.time === "all") return true;

    let now = new Date();
    let t = new Date(p.file.ctime);
    let diff = (now - t) / (1000 * 3600 * 24);

    if (state.time === "week") return diff <= 7;
    if (state.time === "month") return diff <= 30;
    if (state.time === "year") return diff <= 365;
}

// ================== 封面处理（终极安全版） ==================
function getCoverUrl(p) {
    if (!p.cover) return null;

    let cover = p.cover;

    // Dataview 可能返回数组
    if (Array.isArray(cover)) {
        cover = cover[0];
    }

    let file = null;

    // [[xxx]] 形式
    if (cover?.path) {
        file = app.metadataCache.getFirstLinkpathDest(cover.path, p.file.path);
    }
    // 字符串路径
    else if (typeof cover === "string") {
        file = app.metadataCache.getFirstLinkpathDest(cover, p.file.path);
    }

    if (file) {
        return app.vault.getResourcePath(file);
    }

    return null;
}

// ================== 构建筛选 ==================
function buildFilters() {
    filter.innerHTML = "";

    // 类型
    let g1 = filter.createEl("div", { cls: "group" });
    createBtn(g1, "全部", "type", "all");
    TYPES.forEach(t => createBtn(g1, t, "type", t));

    // 评分
    let g2 = filter.createEl("div", { cls: "group" });
    createBtn(g2, "全部", "rating", "all");
    [10,8,6,4,2].forEach(r => {
        createBtn(g2, "⭐".repeat(r/2), "rating", r);
    });

    // 状态
    let g3 = filter.createEl("div", { cls: "group" });
    createBtn(g3, "全部", "status", "all");
    createBtn(g3, "已完成", "status", "completed");
    createBtn(g3, "进行中", "status", "doing");
    createBtn(g3, "未开始", "status", "planned");

    // 时间
    let g4 = filter.createEl("div", { cls: "group" });
    createBtn(g4, "全部", "time", "all");
    createBtn(g4, "最近一周", "time", "week");
    createBtn(g4, "最近一月", "time", "month");
    createBtn(g4, "最近一年", "time", "year");

    // 排序
    let g5 = filter.createEl("div", { cls: "group" });
    g5.createEl("span", { text: "排序方式：" });
    createBtn(g5, "评分↓", "sort", "rating");
    createBtn(g5, "标题", "sort", "title");
    createBtn(g5, "创建时间", "sort", "ctime");
}

// ================== 渲染 ==================
function render() {
    buildFilters();
    grid.innerHTML = "";

    let list = pages
        .where(p => state.type === "all" || p.type === state.type)
        .where(p => state.status === "all" || p.status === state.status)
        .where(p => state.rating === "all" || (p.rating && p.rating >= state.rating))
        .where(p => timeFilter(p));

    // 排序
    if (state.sort === "rating") list = list.sort(p => p.rating, "desc");
    if (state.sort === "title") list = list.sort(p => p.file.name);
    if (state.sort === "ctime") list = list.sort(p => p.file.ctime, "desc");

    list.forEach(p => {
        let card = grid.createEl("div", { cls: "card" });

        let coverWrap = card.createEl("div", { cls: "cover-wrap" });

        // ===== 封面 =====
        let url = getCoverUrl(p);
        if (url) {
            coverWrap.createEl("img", {
                attr: { src: url },
                cls: "cover"
            });
        } else {
            coverWrap.createEl("div", {
                text: "No Cover",
                cls: "no-cover"
            });
        }

        // ===== 状态 =====
        if (p.status === "completed") {
            coverWrap.createEl("div", { text: "✓", cls: "badge done" });
        }

        // ===== 类型 =====
        coverWrap.createEl("div", {
            text: p.type,
            cls: "badge type"
        });

        // ===== ⭐评分 =====
        if (p.rating) {
            coverWrap.createEl("div", {
                text: "⭐".repeat(Math.round(p.rating/2)),
                cls: "rating"
            });
        }

        // ===== 标题 =====
        card.createEl("div", {
            text: p.file.name,
            cls: "title"
        });

        // 作者
        if (p.author) {
            card.createEl("div", {
                text: p.author,
                cls: "author"
            });
        }
    });
}

render();

// ================== 样式 ==================
const style = document.createElement("style");
style.textContent = `

.group {
    margin-bottom: 8px;
}

.btn {
    margin: 4px;
    padding: 5px 12px;
    border-radius: 20px;
    border: none;
    background: #eee;
    cursor: pointer;
}

.btn.active {
    background: #5b5bf7;
    color: white;
}

.grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(170px, 1fr));
    gap: 16px;
}

.card {  
    background: transparent;   /* 去掉灰色背景 */  
    border-radius: 12px;  
    overflow: hidden;  
}  
  
/* ===== 封面容器 ===== */  
.cover-wrap {  
    position: relative;  
    width: 100%;  
    height: 260px;   /* 可以调大一点更像你截图 */  
}  
  
/* ===== 封面图（核心！） ===== */  
.cover {  
    width: 100%;  
    height: 100%;  
    object-fit: cover;   /* 关键：铺满 + 裁剪 */  
    display: block;  
}  
  
/* ===== 去掉之前的空白占位 ===== */  
.no-cover {  
    width: 100%;  
    height: 100%;  
    background: #ddd;  
}  
  
/* ===== 标签 ===== */  
.badge {  
    position: absolute;  
    top: 8px;  
    padding: 2px 6px;  
    border-radius: 6px;  
    font-size: 12px;  
    color: white;  
}  
  
.done {  
    left: 8px;  
    background: green;  
}  
  
.type {  
    right: 8px;  
    background: purple;  
}  
  
/* ===== ⭐评分（覆盖在图上） ===== */  
.rating {  
    position: absolute;  
    bottom: 8px;  
    left: 8px;  
    background: rgba(0,0,0,0.7);  
    padding: 3px 6px;  
    border-radius: 6px;  
    font-size: 12px;  
}  
  
/* ===== 标题（更紧凑） ===== */  
.title {  
    margin-top: 6px;  
    font-weight: bold;  
    font-size: 14px;  
}  
  
/* ===== 作者 ===== */  
.author {  
    font-size: 12px;  
    color: gray;  
}

---

# 🔥 如果你想更像你截图（推荐再加一个）

👉 加一个“封面 hover 放大”效果（很像 Notion）：

.cover-wrap:hover .cover {  
    transform: scale(1.05);  
    transition: 0.3s;  
}

document.head.appendChild(style);
```