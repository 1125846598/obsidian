```dataviewjs
// ================== 配置 ==================
const TYPES = ["book", "movie", "teleplay", "anime", "comic", "game"];
const NS = "mlib";

let state = {
    type: "all",
    status: "all",
    rating: "all"
};

// ================== 容器 ==================
const container = dv.container;
container.innerHTML = "";
container.classList.add(`${NS}-root`);

const filter = container.createEl("div");
filter.className = `${NS}-filter`;

const grid = container.createEl("div");
grid.className = `${NS}-grid`;

// ================== 数据 ==================
let pages = dv.pages().where(p => p.type);

// ================== 按钮 ==================
function btn(parent, text, key, value) {
    let b = parent.createEl("button", { text });
    b.className = `${NS}-btn`;
    if (state[key] === value) b.classList.add(`${NS}-btn--active`);
    b.onclick = () => {
        state[key] = value;
        render();
    };
}

// ================== 封面 ==================
function getCover(p) {
    if (!p.cover) return null;
    let c = p.cover;
    if (Array.isArray(c)) c = c[0];
    let file = null;
    if (c?.path) file = app.metadataCache.getFirstLinkpathDest(c.path, p.file.path);
    else if (typeof c === "string") file = app.metadataCache.getFirstLinkpathDest(c, p.file.path);
    if (file) return app.vault.getResourcePath(file);
    return null;
}

// ================== 渲染 ==================
function render() {
    filter.innerHTML = "";
    grid.innerHTML = "";

    // 类型筛选
    const typeRow = filter.createEl("div");
    typeRow.className = `${NS}-filter-row`;
    btn(typeRow, "全部", "type", "all");
    TYPES.forEach(t => btn(typeRow, t, "type", t));

    // 状态筛选
    const statusRow = filter.createEl("div");
    statusRow.className = `${NS}-filter-row`;
    btn(statusRow, "全部状态", "status", "all");
    btn(statusRow, "✓ 已完成", "status", "completed");
    btn(statusRow, "▷ 未完成", "status", "incomplete");

    // 评分筛选
    const ratingRow = filter.createEl("div");
    ratingRow.className = `${NS}-filter-row`;
    btn(ratingRow, "全部评分", "rating", "all");
    [4, 6, 8, 10].forEach(r => btn(ratingRow, "⭐".repeat(r / 2), "rating", r));

    // 过滤数据
    let list = pages
        .where(p => state.type === "all" || p.type === state.type)
        .where(p => state.status === "all"
                 || (state.status === "completed" && p.status === "completed")
                 || (state.status === "incomplete" && p.status !== "completed"))
        .where(p => state.rating === "all" || (p.rating && p.rating >= state.rating));

    // 渲染卡片
    list.forEach(p => {
        let card = grid.createEl("div");
        card.className = `${NS}-card`;

        let url = getCover(p);
        let wrap = card.createEl("div");
        wrap.className = `${NS}-wrap`;

        if (url) {
            let img = wrap.createEl("img");
            img.className = `${NS}-img`;
            img.setAttribute("src", url);
        } else {
            let placeholder = wrap.createEl("div");
            placeholder.className = `${NS}-placeholder`;
            placeholder.innerText = p.file.name.charAt(0);
        }

        wrap.createEl("div").className = `${NS}-gradient`;

        let tag = wrap.createEl("div");
        tag.className = `${NS}-tag`;
        tag.innerText = p.type;

        if (p.rating) {
            let rating = wrap.createEl("div");
            rating.className = `${NS}-rating`;
            rating.innerText = "⭐".repeat(Math.round(p.rating / 2));
        }

        if (p.status === "completed") {
            let done = wrap.createEl("div");
            done.className = `${NS}-done`;
            done.innerText = "✓";
        }

        // 封面下方信息
        let info = card.createEl("div");
        info.className = `${NS}-info`;

        let title = info.createEl("div");
        title.className = `${NS}-title`;
        title.innerText = p.file.name;

        if (p.author) {
            let author = info.createEl("div");
            author.className = `${NS}-author`;
            author.innerText = p.author;
        }
    });
}

render();

// ================== 样式 ==================
const STYLE_ID = `${NS}-style`;
if (!document.getElementById(STYLE_ID)) {
    const style = document.createElement("style");
    style.id = STYLE_ID;
    style.textContent = `

.${NS}-root {
    font-family: inherit;
}

.${NS}-filter {
    display: flex;
    flex-direction: column;
    gap: 8px;
    margin-bottom: 16px;
}

.${NS}-filter-row {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
}

.${NS}-btn {
    padding: 4px 12px;
    border-radius: 20px;
    border: 1px solid #ccc;
    background: transparent;
    cursor: pointer;
    font-size: 13px;
    transition: background 0.15s, color 0.15s;
}

.${NS}-btn--active {
    background: #5b5bf7 !important;
    color: white !important;
    border-color: #5b5bf7 !important;
}

.${NS}-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
    gap: 20px;
}

.${NS}-card {
    border-radius: 18px;
    overflow: hidden;
}

.${NS}-wrap {
    position: relative;
    width: 100%;
    height: 260px;
    background: #2a2a2a;
    border-radius: 18px;
    overflow: hidden;
}

.${NS}-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
}

.${NS}-placeholder {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 48px;
    font-weight: bold;
    color: #666;
    background: #1e1e2e;
}

.${NS}-gradient {
    position: absolute;
    bottom: 0;
    width: 100%;
    height: 30%;
    background: linear-gradient(to top, rgba(0,0,0,0.6), transparent);
    pointer-events: none;
}

.${NS}-tag {
    position: absolute;
    top: 10px;
    right: 10px;
    background: rgba(0,0,0,0.6);
    color: white;
    padding: 4px 8px;
    border-radius: 10px;
    font-size: 12px;
}

.${NS}-rating {
    position: absolute;
    bottom: 8px;
    left: 10px;
    font-size: 13px;
}

.${NS}-done {
    position: absolute;
    top: 10px;
    left: 10px;
    background: #22c55e;
    color: white;
    padding: 2px 7px;
    border-radius: 6px;
    font-size: 12px;
    font-weight: bold;
}

.${NS}-info {
    padding: 6px 4px 2px 4px;
}

.${NS}-title {
    font-weight: bold;
    font-size: 14px;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
}

.${NS}-author {
    font-size: 12px;
    color: #888;
    margin-top: 2px;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
}

    `;
    document.head.appendChild(style);
}
```